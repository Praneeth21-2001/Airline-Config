server:
  port: 5004

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/airline_location_db
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

