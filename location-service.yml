server:
  port: 5004


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/airline_location_db
    username: root
    password: admin


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

