server:
  port: 5006


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/airline_pricing_db
    username: root
    password: admin


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true